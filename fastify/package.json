{
  "name": "fastify",
  "main": "app.js",
  "scripts": {
    "start": "fastify start -l info app.js -p 3003",
    "dev": "fastify start -w -l info -P app.js"
  },
  "dependencies": {
    "@fastify/autoload": "^5.0.0",
    "fastify": "^4.26.1",
    "fastify-cli": "^6.2.1",
    "mongodb": "^6.8.0"
  },
  "devDependencies": {}
}
